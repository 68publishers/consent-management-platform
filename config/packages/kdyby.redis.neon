extensions:
    kdyby.redis: Kdyby\Redis\DI\RedisExtension

kdyby.redis:
    session:
        database: 0
        native: no

services:
    redis:
        type: Kdyby\Redis\RedisStorage
        autowired: self

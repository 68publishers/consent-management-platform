{extends $originalTemplate}

{varType App\ReadModel\Consent\ConsentView $item}

{define col-settings_checksum}
    {if NULL !== $item->settingsChecksum}
        <span x-data x-tooltip.interactive.html.raw="<span class='break-words'>{$item->settingsChecksum->value()}</span>">{$item->settingsChecksum->value()|substr:0, 8}</span>
    {/if}
{/define}
